# Exported configuration and reports (may contain tenant-specific data)
mdo-export/
*.json
*.xlsx
*.csv

# Tenant config templates ship with empty UPNs and ARE tracked. Fill them in locally; to keep your
# real UPNs out of git, run: git update-index --skip-worktree config/tenants.json
!config/tenants.json
!config/tenants.example.json

# Orchestrator run logs
logs/

# PowerShell / editor noise
*.log
.vscode/
.idea/
